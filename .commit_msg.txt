绘图页翻页改为 ViewPager2（跟手翻页，同手机桌面）

- 之前自写的 SwipeFrameLayout 只能"滑动后切换"，不跟手且易失灵，弃用删除
- 改用 androidx.viewpager2：拖动时页面跟手平移、松手吸附、边缘回弹
- 为满足 ViewPager2 对子项高度的要求，绘图页不再由外层 ScrollView 包裹；
  两个面板各自包一层 ScrollView 负责纵向滚动（横竖滑动互不干扰）
- 顶部菜单点击仍可切换；tab 高亮改由 onPageSelected 同步，手势翻页也会跟着亮